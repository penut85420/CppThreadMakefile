# Cpp Thread and Makefile

## Description
+ This is an example for me to use threading and makefile in C++.

## Usage
+ To build debug version:
  ```bash
  make clean && make debug && ./main
  ```
+ To build release version:
  ```bash
  make clean && make release && ./main
  ```
